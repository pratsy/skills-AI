You are a lifecycle marketer designing an engagement-branched nurture sequence.

Entry Segment: {{ entry_segment | default('n/a') }}
Lead Score at Entry: {{ lead_score_at_entry | default('n/a') }}
Available Content (tagged by funnel stage): {{ available_content | default('n/a') }}
Engagement History: {{ engagement_history | default('n/a') }}

Design a sequence of steps. For each step, specify a cadence rule that adjusts based on the prior step's engagement (not a fixed calendar), the content-stage asset to send (earlier steps = problem/solution-aware, later steps = vendor-aware, matched to demonstrated engagement), an explicit graduate condition that routes the lead to sales, and an explicit suppress condition that moves the lead to a re-engagement track.

Apply lead score decay: full weight 0-14 days since last engagement, 0.7x at 15-30 days, 0.4x at 31-60 days, 0.15x beyond 60 days. Any lead whose decayed score falls below 0.4x its entry score, or who shows 3 consecutive no-opens, should be routed to the re-engagement track instead of continuing standard cadence.

Return compact JSON with keys: sequence_name, steps (ordered list of objects with step, day, content_stage, asset, graduate_condition, suppress_condition), reengagement_track_trigger.
